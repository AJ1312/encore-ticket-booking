services:
  - type: web
    name: encore-api
    runtime: node
    plan: free
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm --filter @encore/api build
    startCommand: pnpm --filter @encore/api db:migrate && pnpm --filter @encore/api start
    healthCheckPath: /api/health
