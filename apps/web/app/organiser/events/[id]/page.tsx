import Link from 'next/link';
import { ArrowLeft, ArrowUpRight } from 'lucide-react';
import { PortalFooter } from '@/components/portal-footer';
import { PortalNav } from '@/components/portal-nav';
export default async function EditEventPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="portal-page organiser"><PortalNav portal="organiser"/><section className="portal-content compact"><Link href="/organiser/events" className="back-link"><ArrowLeft size={15}/> Events</Link><span className="eyebrow">Organiser / Event {id}</span><h1>The Night<br/><em>We Remember.</em></h1><div className="admin-cards"><section className="portal-panel"><span className="eyebrow">Event details</span><h2>On sale · 68% filled</h2><p>Open-air concert at Riverside Grounds with one show and three pricing bands.</p><Link href={`/organiser/events/${id}/shows`}>Manage shows <ArrowUpRight size={15}/></Link></section><section className="portal-panel"><span className="eyebrow">FairHold</span><h2>Healthy queue.</h2><p>0 double bookings, 1,842 successful seat decisions, and a 94.6 CFI.</p><Link href="/admin/contention-lab">Open lab <ArrowUpRight size={15}/></Link></section></div></section><PortalFooter/></main>; }
