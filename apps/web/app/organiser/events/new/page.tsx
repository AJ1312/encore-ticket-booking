'use client';
import Link from 'next/link';
import { ArrowLeft, Save } from 'lucide-react';
import { PortalFooter } from '@/components/portal-footer';
import { PortalNav } from '@/components/portal-nav';
import { apiJson } from '@/lib/api';
import { useState } from 'react';
export default function NewEventPage() { const [error, setError] = useState(''); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); try { await apiJson('/organiser/events', { method: 'POST', body: JSON.stringify({ title: form.get('title'), description: form.get('description'), type: form.get('type'), posterUrl: form.get('posterUrl') }) }); window.location.href = '/organiser/events'; } catch { setError('Unable to save event. Check organiser access and form details.'); } } return <main className="portal-page organiser"><PortalNav portal="organiser"/><section className="portal-content compact"><Link href="/organiser/events" className="back-link"><ArrowLeft size={15}/> Events</Link><span className="eyebrow">Organiser / Create event</span><h1>Give it a<br/><em>name.</em></h1><form className="admin-form" onSubmit={submit}><label>Title<input name="title" required placeholder="e.g. Neon Horizon Live"/></label><label>Description<textarea name="description" required placeholder="Tell people why this night matters."/></label><label>Type<select name="type" defaultValue="concert"><option value="concert">Concert</option><option value="comedy">Comedy</option><option value="movie">Movie</option><option value="other">Other</option></select></label><label>Poster URL<input name="posterUrl" required type="url" placeholder="https://..."/></label>{error && <p className="form-error">{error}</p>}<button className="coral-button"><Save size={15}/> Save event</button></form></section><PortalFooter/></main>; }
