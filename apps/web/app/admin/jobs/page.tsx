import { PortalFooter } from '@/components/portal-footer';
import { PortalNav } from '@/components/portal-nav';
export default function AdminJobsPage() { return <main className="portal-page admin"><PortalNav portal="admin"/><section className="portal-content compact"><span className="eyebrow">System control / Durable queue</span><h1>Jobs that<br/><em>keep moving.</em></h1><div className="metric-row"><div><small>Pending</small><strong>03</strong><span>Available now</span></div><div><small>Completed</small><strong>1,842</strong><span>Last 30 days</span></div><div><small>Failed</small><strong>00</strong><span>Healthy</span></div></div><div className="event-table admin-table">{['send-booking-confirmation','release-expired-holds','waitlist-cascade'].map((job, index) => <div className="event-table-row" key={job}><span>0{index + 1}</span><div><strong>{job}</strong><small>Postgres durable queue · attempt 1</small></div><b>{index === 0 ? 'Pending' : 'Completed'}</b><span>⋯</span></div>)}</div></section><PortalFooter/></main>; }
